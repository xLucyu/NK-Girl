import { DailyChallengeBody, MetaBody } from "@btd6";

export interface ChallengeProfileProps {
  event?: DailyChallengeBody
  metaData: MetaBody;
}

export function ChallengeProfile({ event, metaData }: ChallengeProfileProps): JSX.Element {
  return (
    <div>

    </div>
  )
}